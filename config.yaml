splunk:
  host: localhost
  port: 8089
  username: 
  password: 
  verify_ssl: 

demo_mode: false

ai:
  provider: gemini
  gemini_model: gemini-1.5-flash

agent:
  max_investigation_steps: 10
  confidence_threshold: 0.6

output:
  reports_dir: ./reports
  logs_dir: ./logs
