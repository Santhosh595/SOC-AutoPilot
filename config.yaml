splunk:
  host: localhost
  port: 8089
  username: 
  password: 
  verify_ssl: 

demo_mode: false

ai:
  provider: gemini
  gemini_model: gemini-1.5-flash

agent:
  max_investigation_steps: 10
  confidence_threshold: 0.6

output:
  reports_dir: ./reports
  logs_dir: ./logs

notifications:
  slack_webhook: ""     # Paste Slack Incoming Webhook URL here
  email_to: ""          # Recipient email address
  email_from: ""        # Sender email address (Gmail recommended)
  smtp_host: smtp.gmail.com
  smtp_port: 587
